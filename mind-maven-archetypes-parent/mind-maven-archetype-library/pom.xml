<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.ow2.mind</groupId>
		<artifactId>mind-maven-archetypes-parent</artifactId>
		<version>2.1-SNAPSHOT</version>
	</parent>

	<artifactId>mind-maven-archetype-library</artifactId>
	<packaging>maven-plugin</packaging>
	<name>Mind components library Maven archetype</name>

	<description>
		This archetype is useful to quick start Mind components library
		projects, having a source tree template already filled.
	</description>

	<!-- Because of a bug in the maven-site-plugin, the URL of the project MUST 
		ends with a '/' which is not the case when using the basic maven
		inheritance mechanism -->
	<url>${mind.home.url}/maven-parent/${project.parent.artifactId}/${project.artifactId}/</url>

	<scm>
		<connection>scm:svn:svn://svn.forge.objectweb.org/svnroot/fractal/tags/mind-2.1.0/maven/archetypes/maven-archetype-mind-library</connection>
		<developerConnection>scm:svn:svn+ssh://svn.forge.objectweb.org/svnroot/fractal/tags/mind-2.1.0/maven/archetypes/maven-archetype-mind-library</developerConnection>
		<url>http://svn.forge.objectweb.org/cgi-bin/viewcvs.cgi/fractal/tags/mind-2.1.0/maven/archetypes/maven-archetype-mind-library</url>
	</scm>

	<developers>
		<developer>
			<id>apace</id>
			<name>Alessio Pace</name>
			<email>alessio.pace@gmail.com</email>
		</developer>
	</developers>

	<dependencies>
		<!-- Following dependencies are declared for integration tests since 
			they are used for building modules generated by this archetype. -->
		<!-- <dependency>
			<groupId>org.objectweb.fractal.mind</groupId>
			<artifactId>mind-baselib</artifactId>
			<version>${project.version}</version>
			<type>mar</type>
			<scope>test</scope>
		</dependency> -->

		<dependency>
			<groupId>org.ow2.mind</groupId>
			<artifactId>mind-lifecycle-maven-plugin</artifactId>
			<version>${project.version}</version>
			<type>maven-plugin</type>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Use an ant script to make the replacement of version value in 
				archatype pom.xml file. Using maven filtering would make
				unexpected replacement. -->
			<plugin>
			    <groupId>org.apache.maven.plugins</groupId>
            	<artifactId>maven-plugin-plugin</artifactId>
            	<version>3.1</version>
            	<configuration>
                	<skipErrorNoDescriptorsFound>true</skipErrorNoDescriptorsFound>
            	</configuration>
            </plugin>
            
			<!-- <plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<executions>
					<execution>
						<id>process plugin.xml</id>
						<phase>process-resources</phase>
						<configuration>
							<tasks>
								<replace dir="${project.build.outputDirectory}" includes="**/pom.xml" token="@version@" value="${project.version}" summary="true" />
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin> -->
		</plugins>
	</build>

	<profiles>
		<!-- The integration-test profile is always activated unless the 
			"integration-test.skip" property is defined. -->
		<profile>
			<id>integration-test</id>
			<activation>
				<property>
					<name>!integration-test.skip</name>
				</property>
			</activation>
			<build>
				<plugins>
					<!-- Need to install IT snapshot of plugin -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-install-plugin</artifactId>
						<executions>
							<execution>
								<id>integration-test</id>
								<phase>pre-integration-test</phase>
								<goals>
									<goal>install-file</goal>
								</goals>
								<configuration>
									<file>
										${project.build.directory}/${project.build.finalName}.jar
									</file>
									<version>it-SNAPSHOT</version>
									<packaging>maven-plugin</packaging>
									<pomFile>${basedir}/pom.xml</pomFile>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<!-- to "use" the archetype -->
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-archetype-plugin</artifactId>
						<executions>
							<execution>
								<id>integration-test</id>
								<phase>integration-test</phase>
								<goals>
									<goal>create</goal>
								</goals>
								<configuration>
									<archetypeGroupId>
										${project.groupId}
									</archetypeGroupId>
									<archetypeArtifactId>
										${project.artifactId}
									</archetypeArtifactId>
									<archetypeVersion>
										it-SNAPSHOT
									</archetypeVersion>

									<groupId>
										org.objectweb.fractal.mind.maven-archetype-mind-library.test
									</groupId>
									<artifactId>test-module</artifactId>
									<version>1.0-SNAPSHOT</version>

									<basedir>
										${project.build.testOutputDirectory}/it
									</basedir>
								</configuration>
							</execution>
						</executions>
					</plugin>

					<!-- Execute pom.xml into target/test-classes/it -->
					<!--<plugin>
						<artifactId>maven-invoker-plugin</artifactId>
						<executions>
							<execution>
								<id>integration-test</id>
								<phase>integration-test</phase>
								<goals>
									<goal>run</goal>
								</goals>
								<configuration>
									<projectsDirectory>
										${project.build.testOutputDirectory}/it
									</projectsDirectory>
									<pomIncludes>
										<pomInclude>
											test-module/pom.xml
										</pomInclude>
									</pomIncludes>
									<debug>true</debug>
									<postBuildHookScript>
										../../verifyArchetype.bsh
									</postBuildHookScript>
								</configuration>
							</execution>
						</executions>
					</plugin>-->

				</plugins>
			</build>
		</profile>
	</profiles>

</project>